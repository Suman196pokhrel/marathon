# Aliasing

Two or more names bound to the same object — that's all it is. Not a feature, not a bug; it's the direct consequence of assignment binding rather than copying.

```python
a = [1, 2]
b = a           # alias: b and a now reference the same list
```
